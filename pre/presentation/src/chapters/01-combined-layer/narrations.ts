import type { Narration } from "../../registry/types";

export const narrations: Narration[] = [
  "User input is messy. The Combined NLP Layer cleans it first, then routes it fast.",
  "In our XMUM freshman FAQ chatbot, intent detection splits the flow in two. Clear rule matches return a direct reply, while FAQ queries continue to category classification and retrieval.",
];
